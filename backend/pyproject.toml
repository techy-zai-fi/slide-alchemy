[project]
name = "slide-alchemy-backend"
version = "0.1.0"
description = "SlideAlchemy FastAPI backend"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
